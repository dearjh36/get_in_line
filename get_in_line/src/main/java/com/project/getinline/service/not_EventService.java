/*package com.project.getinline.service;

import com.project.getinline.constant.EventStatus;
import com.project.getinline.dto.EventDTO;

import java.time.LocalDateTime;
import java.util.List;

/**
 * 이벤트 서비스
 *
 * @author dear
 *
public interface EventService {

    /**
     * 검색이름을 받아서 이벤트 리스트를 반환
     *
     * @param placeId 장소ID
     * @param eventName 이벤트 이름
     * @param eventStatus 이벤트 상태
     * @param eventStartDatetime 시작시간
     * @param eventEndDatetime 종료시간
     *

    List<EventDTO> findEvents(
            Long placeId,
            String eventName,
            EventStatus eventStatus,
            LocalDateTime eventStartDatetime,
            LocalDateTime eventEndDatetime
    );
}

현재 'EventService' 인터페이스 중간에 둬서 EventServiceImpl 클래스를
생성할 계획이었으나 바로 서비스에서 구현


 */
